// Fonts configuration
// Run "npx react-native link" after adding new fonts files
// Then re-run "npx react-native run-android" to initialise with the newly bundled resources
module.exports = {
	project: {
		ios: {},
		android: {},
	},
	assets: ['./src/assets/fonts'],
};